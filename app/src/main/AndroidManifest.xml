<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.e_channelling">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EChannelling">

        <activity android:name=".Patient_SIGN_IN">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Member_Card">
        </activity>
        <activity android:name=".Feedback">
        </activity>
        <activity android:name=".Patient_Profile">
        </activity>
        <activity android:name=".Patient_Details">
        </activity>
        <activity android:name=".Edit_Details">
        </activity>
        <activity android:name=".Patient_Dashboard">
        </activity>
        <activity android:name=".ViewPatientActivity">
        </activity>
        <activity android:name=".Select_Doctor">
        </activity>

    </application>

</manifest>